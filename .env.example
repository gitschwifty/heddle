# Shared config — loaded by both runtime and bun test
TEST_MODEL=openrouter/free

# Create .env.local for runtime secrets (not loaded by bun test)
# OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Create .env.test for test secrets (auto-loaded by bun test)
# OPENROUTER_API_KEY=sk-or-v1-your-key-here
# TEST_MODEL=openrouter/free
