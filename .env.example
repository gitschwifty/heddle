# Shared config for runtime and integration tests
TEST_MODEL=openrouter/free

# Optional runtime override for CI/headless. Local interactive use should prefer
# macOS Keychain: security add-generic-password -U -s heddle -a openrouter -w
# OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Create .env.test for test secrets loaded by tests/common/env.rs
# OPENROUTER_API_KEY=sk-or-v1-your-key-here
# TEST_MODEL=openrouter/free
